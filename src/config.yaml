# Standalone EXIF stage defaults; this replaces the detector parent's config.
INPUT_DIR: /images
MD_FILE: md_out.json
EN_FILE: mewc_out.pkl
EN_CSV: mewc_out.csv
EXIF_DIR: camelot
METADATA_DIR: metadata
OVERLAP: 0.3
EDGE_DIST: 0.02
MIN_EDGES: 0
UPPER_CONF: 0.9
LOWER_CONF: 0.05
SUPPRESSION_POLICY: category-confidence-v1
